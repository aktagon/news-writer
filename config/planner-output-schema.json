{
  "type": "object",
  "properties": {
    "title": {"type": "string"},
    "deck": {"type": "string", "description": "Brief one-sentence summary of the article"},
    "key_points": {"type": "array", "items": {"type": "string"}},
    "structure": {"type": "array", "items": {"type": "string"}},
    "categories": {"type": "array", "items": {"type": "string"}, "description": "Leaf categories using forward slash format (e.g., Development/Programming, Technology/Innovation)"},
    "tags": {"type": "array", "items": {"type": "string"}, "description": "Relevant tags (e.g., javascript, react, api, performance)"},
    "target": {
      "type": "object",
      "properties": {
        "word_count": {"type": "integer"},
        "tone": {"type": "string"},
        "audience": {"type": "string", "description": "Target audience description (e.g., 'Developers and tech leads evaluating LLM capabilities for production use')"}
      },
      "required": ["word_count", "tone", "audience"]
    }
  },
  "required": ["title", "deck", "key_points", "structure", "categories", "tags", "target"]
}